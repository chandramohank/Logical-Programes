using System;

namespace LogicalProgramingQuestions
{
    ////public static class CrossProduct2DArray
    ////{
    ////    public static string[] GetCartcieanAray(string[] left, string[] rest, int index)
    ////    {
    ////        if (rest.Length == index)
    ////        {
    ////            Console.WriteLine(left);
    ////            return left;
    ////        }

    ////        var current = rest[index];
    ////        for (var i = 0; i < current.Length; ++i)
    ////        {
    ////            left.push(current[i]);
    ////            GetCartcieanAray(left, rest, index + 1);
    ////            left.pop();
    ////        }
    ////    }
    ////}
}
